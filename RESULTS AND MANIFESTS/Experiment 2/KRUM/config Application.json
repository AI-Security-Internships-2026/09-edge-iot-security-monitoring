{
  "run_id": "application_he_krum_hybrid_v1",
  "experiment": "Experiment 2 — HE vs. Krum (head-only classifier attack, corrected)",
  "model_type": "application",
  "num_clients": 10,
  "num_rounds": 25,
  "local_epochs": 5,
  "learning_rate": 0.001,
  "prox_mu": 0.02,
  "dirichlet_alpha": 0.7,
  "batch_size": 512,

  "use_krum": false,
  "use_adaptive_krum": false,
  "use_he": false,
  "use_he_krum_hybrid": true,
  "use_dp": false,
  "use_zkp": false,

  "he_poly_degree": 8192,
  "he_coeff_mod_bit_sizes": [60, 40, 40, 60],
  "he_global_scale": 1099511627776,
  "he_sensitive_prefix": "classifier",
  "he_pct_encrypted_measured": 5.8,

  "adaptive_krum_method": "mad",
  "adaptive_krum_k": 2.5,
  "adaptive_krum_min_keep_fraction": 0.5,
  "adaptive_krum_scores_which_slice": "bulk (plaintext) only",

  "byzantine_clients": [0, 1],
  "byzantine_head_only": true,
  "attack_scale": 2.0,
  "attack_type": "classifier_head_flip_attack (corrected: full local training first, then flip classifier.* keys only)",

  "device": "cpu",
  "cuda_available": false,
  "parallelism": "ProcessPoolExecutor, 4 workers, 3 threads/worker (12 cores detected)",

  "code_state": {
    "note": "No git commit hash captured for this run — main.py was edited in place and copied to the DGX/local machine via git push/pull and scp during this session, not committed at time of run. Fill in git_commit once these changes are actually committed.",
    "git_commit": null,
    "fixes_present_in_this_run": [
      "classifier_head_flip_attack call site trains the full model locally first, then flips only classifier.* keys (corrected from an earlier version that skipped local training entirely and made detection trivial for an unrelated reason)",
      "USE_HE_KRUM_HYBRID hybrid aggregation branch: adaptive Krum scores bulk/plaintext slice only; he_local.aggregate_encrypted restricted to Krum-selected clients for the classifier-head ciphertext",
      "Byzantine console tag correctly prints 'head-only' (checks USE_HE_KRUM_HYBRID, not just USE_HE)",
      "adaptive_multi_krum() in defences/krum.py: hard check message matches its actual condition (f<=n-3); Blanchard n>=2f+3 bound is a warning, not a raise; min_keep_fraction=0 actually disables the floor",
      "main.py's two Krum-fallback pre-check guards match adaptive_multi_krum()'s actual hard threshold (n>=f+3), not the stricter Blanchard bound"
    ],
    "known_unresolved_items_not_affecting_this_run": [
      "defences/krum.py has no multi_krum() (fixed-m) function — irrelevant here since USE_KRUM=False",
      "he_aggregate() (the OLD full-model USE_HE path in main.py) still has its two original bugs (no decrypt-before-return, unweighted averaging) — irrelevant here since this run used the separate USE_HE_KRUM_HYBRID/he_local.py path, not that one"
    ]
  },

  "run_command": "python main.py application",
  "note_no_tag_used": "This run was launched without --tag, so output files use the bare 'application' name (results_application.csv, checkpoint_application.npz, checkpoint_application_best.npz, checkpoint_application_progress.json) rather than a tagged name. Recommend --tag he_krum_hybrid_app for any repeat run to avoid collision with other application-model runs.",

  "environment": {
    "machine": "Local Windows machine (not the DGX Spark) — venv at VIRTUAL ENVs/.venv311",
    "os": "Windows",
    "python_version_caveat": "venv is named .venv311 but pip's own upgrade hint pointed at pythoncore-3.14-64 — actual interpreter version should be double-checked with `python --version`; likely Python 3.14, not 3.11, despite the folder name",
    "tenseal_install_path": "python -m pip install tenseal (succeeded on this machine after a typo fix; TenSEAL has no official Windows wheel, so this install's exact provenance — prebuilt wheel vs local build — should be confirmed and noted, since it differs from the DGX ARM64 source-build path used earlier in this project)"
  },


  ]
}
